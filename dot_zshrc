# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH

# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time Oh My Zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="robbyrussell"

# Set list of themes to pick from when loading at random
# Setting this variable when ZSH_THEME=random will cause zsh to load
# a theme from this variable instead of looking in $ZSH/themes/
# If set to an empty array, this variable will have no effect.
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

# Uncomment the following line to use case-sensitive completion.
# CASE_SENSITIVE="true"

# Uncomment the following line to use hyphen-insensitive completion.
# Case-sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment one of the following lines to change the auto-update behavior
# zstyle ':omz:update' mode disabled  # disable automatic updates
# zstyle ':omz:update' mode auto      # update automatically without asking
# zstyle ':omz:update' mode reminder  # just remind me to update when it's time

# Uncomment the following line to change how often to auto-update (in days).
# zstyle ':omz:update' frequency 13

# Uncomment the following line if pasting URLs and other text is messed up.
# DISABLE_MAGIC_FUNCTIONS="true"

# Uncomment the following line to disable colors in ls.
# DISABLE_LS_COLORS="true"

# Uncomment the following line to disable auto-setting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment the following line to enable command auto-correction.
# ENABLE_CORRECTION="true"

# Uncomment the following line to display red dots whilst waiting for completion.
# You can also set it to another string to have that shown instead of the default red dots.
# e.g. COMPLETION_WAITING_DOTS="%F{yellow}waiting...%f"
# Caution: this setting can cause issues with multiline prompts in zsh < 5.7.1 (see #5765)
# COMPLETION_WAITING_DOTS="true"

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Uncomment the following line if you want to change the command execution time
# stamp shown in the history command output.
# You can set one of the optional three formats:
# "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# or set a custom format using the strftime function format specifications,
# see 'man strftime' for details.
# HIST_STAMPS="mm/dd/yyyy"

# Would you like to use another custom folder than $ZSH/custom?
# ZSH_CUSTOM=/path/to/new-custom-folder

# Which PLUGINS would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.

plugins=(git docker zsh-pre-commit-autocomplete fzf-tab rust ollama chezmoi)

source $ZSH/oh-my-zsh.sh

# User configuration

# export MANPATH="/usr/local/man:$MANPATH"

# You may need to manually set your language environment
# export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='nvim'
# fi

# Compilation flags
# export ARCHFLAGS="-arch $(uname -m)"

# Set personal aliases, overriding those provided by Oh My Zsh libs,
# plugins, and themes. Aliases can be placed here, though Oh My Zsh
# users are encouraged to define aliases within a top-level file in
# the $ZSH_CUSTOM folder, with .zsh extension. Examples:
# - $ZSH_CUSTOM/aliases.zsh
# - $ZSH_CUSTOM/macos.zsh
# For a full list of active aliases, run `alias`.
#
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

# Zoxide to replace cd command with zoxide
eval "$(zoxide init --cmd cd zsh)"

# LibOMP
export PATH="/opt/homebrew/opt/libomp/bin:$PATH"
export LDFLAGS="-L/opt/homebrew/opt/libomp/lib"
export CPPFLAGS="-I/opt/homebrew/opt/libomp/include"

# LLVM
export PATH="/opt/homebrew/opt/llvm/bin:$PATH"
export LDFLAGS="-L/opt/homebrew/opt/llvm/lib"
export CPPFLAGS="-I/opt/homebrew/opt/llvm/include"
export CC=/opt/homebrew/opt/llvm/bin/clang
export CXX=/opt/homebrew/opt/llvm/bin/clang++

# ZSH PLUGINS
source $(brew --prefix)/share/zsh-autosuggestions/zsh-autosuggestions.zsh
source $HOMEBREW_PREFIX/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
source $HOMEBREW_PREFIX/share/zsh-autocomplete/zsh-autocomplete.plugin.zsh

# Set up fzf key bindings and fuzzy completion
source <(fzf --zsh)

# ZSH-AUTOCOMPLETE: Restore default ZSH behavior of up and down keys (down not restored to enter menus – now yes)
bindkey '\e[A' up-line-or-history
bindkey '\eOA' up-line-or-history
bindkey '\e[B' down-line-or-history
bindkey '\eOB' down-line-or-history

# ZSH-AUTOCOMPLETE: Make tab enter menu instead of filling with first completion
bindkey              '^I' menu-select
bindkey "$terminfo[kcbt]" menu-select

# ZSH-AUTOCOMPLETE: avoid "do you wish to see all XXX possibilities"
zstyle ':completion:*' group-name ''

# PyEnv
# PyEnv recommendation to avoid accidentally linking against a Pyenv-provided Python
alias brew='env PATH="${PATH//$(pyenv root)\/shims:/}" brew'

export PYENV_ROOT="$HOME/.pyenv"
[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init - zsh)"

# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/ericsuardi/.lmstudio/bin"

export LDFLAGS="-L/opt/homebrew/opt/llvm/lib"
export CPPFLAGS="-I/opt/homebrew/opt/llvm/include"

export LDFLAGS="-L/opt/homebrew/opt/llvm/lib/unwind -lunwind"
export LDFLAGS="-L/opt/homebrew/opt/llvm/lib/c++ -L/opt/homebrew/opt/llvm/lib/unwind -lunwind"
export PATH="/Users/ericsuardi/Downloads/chromedriver-mac-arm64/chromedriver:$PATH"

fpath+=~/.zfunc; autoload -Uz compinit; compinit
export PATH=$PATH:$HOME/go/bin

# UV shell completions
eval "$(uv generate-shell-completion zsh)"
eval "$(uvx --generate-shell-completion zsh)"

# UV Mod to fix shell completions with uv run
_uv_run_mod() {
    if [[ "$words[2]" == "run" && "$words[CURRENT]" != -* ]]; then
        local venv_binaries
        if [[ -d .venv/bin ]]; then
            venv_binaries=( ${(@f)"$(_call_program files ls -1 .venv/bin 2>/dev/null)"} )
        fi
        
        _alternative \
            'files:filename:_files -g "*.py"' \
            "binaries:venv binary:(($venv_binaries))"
    else
        _uv "$@"
    fi
}
compdef _uv_run_mod uv


# Enable the fuzzy tab search in completions
enable-fzf-tab

# Enable ngrok completions
if command -v ngrok &>/dev/null; then
    eval "$(ngrok completion)"
fi

# For compilers to find libomp and GCC headers
export LDFLAGS="-L/opt/homebrew/opt/libomp/lib"
export CPPFLAGS="-I/opt/homebrew/opt/libomp/include"

export GOOGLE_CLOUD_PROJECT="gen-lang-client-***"
# The following lines have been added by Docker Desktop to enable Docker CLI completions.
fpath=(/Users/ericsuardi/.docker/completions $fpath)
autoload -Uz compinit
compinit
# End of Docker CLI completions

export NUV_INDEX="https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/3/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/4/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/28/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/84/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/105/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/130/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/143/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/260/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/273/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/286/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/295/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/314/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/323/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/332/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/339/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/361/packages/pypi/simple
https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/374/packages/pypi/simple"
# My personal project library for testing packages:
# https://__token__:glpat-***@lab.u-hopper.com/api/v4/projects/390/packages/pypi/simple"

# Added by Antigravity
export PATH="/Users/ericsuardi/.antigravity/antigravity/bin:$PATH"
export PATH="$HOME/.local/bin:$PATH"

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/ericsuardi/Downloads/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/ericsuardi/Downloads/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/ericsuardi/Downloads/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/ericsuardi/Downloads/google-cloud-sdk/completion.zsh.inc'; fi
